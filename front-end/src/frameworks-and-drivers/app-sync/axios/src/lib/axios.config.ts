import axios from 'axios';

//axios.defaults.baseURL = 'https://api.vove-managed.com/';
// axios.defaults.headers.common["Authorization"] = `Bearer ${localStorage.getItem("token")}`;

export { axios };
